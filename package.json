{
  "type": "module",
  "scripts": {
    "dev": "NODE_ENV=dev node --watch dev.ts",
    "build": "node build.ts"
  },
  "dependencies": {
    "@types/node": "^24.6.0",
    "marked": "^16.3.0"
  },
  "prettier": {
    "arrowParens": "always",
    "printWidth": 100,
    "semi": false,
    "singleQuote": true
  }
}
